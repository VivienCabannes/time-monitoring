#!/usr/bin/env python
import sys
from time_monitor import add_message


if len(sys.argv) < 2:
    print('Please enter message')
    sys.exit(1)

for i in range(1, len(sys.argv)):
    add_message(sys.argv[i])
